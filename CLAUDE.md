# CLAUDE.md

**説明・報告・質問は必ず日本語で書く。**

## このプロジェクトについて

アプリ名は「みっかごはん」。冷蔵庫の在庫から3日分の夕飯(主菜・副菜・汁物)を提案する iPhone 向け PWA。
公開アドレス(/kondate/)・リポジトリ名・データベース名(kondate)は変えない。
仕様はすべて `SPEC.md` に書いてある。作業前に必ず `SPEC.md` を読むこと。

## 技術

- TypeScript / React / Vite / vite-plugin-pwa / Dexie(IndexedDB)/ Vitest
- 公開先:GitHub Pages

## コマンド

- 開発サーバー:`npm run dev`
- テスト:`npm run test`
- ビルド:`npm run build`

## 必ず守ること

- `SPEC.md` が正。仕様に書いていないことは勝手に追加せず、先に質問する
- 仕様にあいまいな点や「未決定の点」に当たったら、推測で進めずに質問する
- `SPEC.md` 5章のフェーズを1つずつ進める。1フェーズ終わったら止まって報告する
- 各フェーズの最初に、作る画面とデータの形を提案し、確認を取ってから実装する
- 各フェーズの最後に、iPhone で確認する手順を示す
- AI の API・外部サーバー・解析ツールなど、外部へのネットワーク通信を入れない
- データは端末内だけに保存する
- 自動で読み取った内容(レシート・口頭入力・URL 取り込み)は、必ず確認画面を通してから保存する
- 公開リポジトリなので、個人情報・家族の名前・実際のレシートの中身をコミットしない
- 初期レシピや食材辞書は新しく作ったものにし、既存のレシピサイトの文章を写さない

## 設計のルール

- 点数計算の倍率・上限・頻度の目標などの数値は、1つの設定ファイル(例:`src/config/scoring.ts`)にまとめる
- 献立を選ぶロジック・量の計算・在庫の増減・テキストの読み取りは、画面から分けて純粋関数(同じ入力なら必ず同じ結果を返す関数)で書き、テストを書く
- ランダム性は引数で差し替えられるようにして、テストで結果を固定できるようにする
- 現在の日付は引数で渡せるようにして、テストで任意の日付を試せるようにする
- 在庫の増減は必ず「在庫の動き」として記録し、キャンセルで元に戻せるようにする

## コミュニケーション

- 説明・報告・質問は日本語で行う
- コード内のコメントも日本語で書く
- 専門用語を使うときは、一言の説明を添える
- 報告は、結論を最初に書く

## コードの書き方

- 型をきちんと付ける(`any` は使わない)
- 1つのファイルが大きくなりすぎたら分割する
- 画面の文言はすべて日本語
